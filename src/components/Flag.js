const Flag = ( props ) => {
    return (
        <>
            <h4>One flag component: &emsp; {props.fullName} &emsp; <small><small>_id: {props._id}</small></small></h4>
        </>
    )
}

export default Flag;
